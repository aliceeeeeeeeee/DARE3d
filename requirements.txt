# --------- deep learning core (handled separately for CUDA) ---------
# NOTE: torch / torchvision / torchaudio are installed by install script (see install.sh / install.ps1)

# --------- pytorch (cpu fallback) ---------
# If you want CPU-only pip install, you can uncomment:
# torch==2.0.1+cpu; torchvision==0.15.2+cpu; torchaudio==2.0.2+cpu --find-links https://download.pytorch.org/whl/cpu

# --------- lightning / metrics ---------
lightning>=2.0.0
torchmetrics>=0.11.4

# --------- hydra ---------
hydra-core==1.3.2
hydra-colorlog==1.2.0
hydra-optuna-sweeper==1.2.0

# --------- loggers ---------
mlflow
tensorboard

# --------- others ---------
rootutils
pre-commit
rich
pytest

# --------- dl library -----
monai==1.3.0

# ------ image processing ---
scikit-image
augmend
gputools
scikit-tensor-py3

# Display / UI
napari

# handy libraries
einops
torchgeometry

# numeric / jit
numba==0.59.1
numpy==1.23.4

# optional: if you need Qt backend control for napari, do not pin PyQt here;
# use conda install -c conda-forge pyqt (check in Readme for details)
