_target_: deletme3D.models.components.swinunetr_wrapper.SwinUNETRWrapper
img_size: [${crop_size}, ${crop_size}, ${crop_size}]
in_channels: 3
out_channels: 1
depths: [2, 2, 2, 2]
num_heads: [2, 4, 8, 16]
feature_size: 24
norm_name: "batch"
drop_rate: 0.0
attn_drop_rate: 0.0
dropout_path_rate: 0.0
normalize: true
spatial_dims: 3
downsample: "merging"
use_v2: false
use_checkpoint: true
