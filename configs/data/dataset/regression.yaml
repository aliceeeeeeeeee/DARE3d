_target_: deletme3D.data.components.regress_3dataset.Regress3Dataset

im_folder: null
label_folder: null
input_channels: ${input_channels}
scale_file: ${scale_file}
default_scale: ${default_scale}
target_scale: ${target_scale}
renorm: ${renorm}
order_dim_img: ${order_dim_img}
training: false
steps_per_epoch: ${steps_per_epoch}
load_labels: true
time_axis_padding: ${time_axis_padding}
crop_size: ${crop_size}
angle_representation: ${representation_mode}
